import '../../model/field_log.dart';

class FirebaseLogs {
  Future<void> uploadLog(FieldLog log) async {
    throw UnimplementedError(
      'Add firebase_core, firebase_auth, cloud_firestore, firebase_storage then implement upload.',
      // Replace later
    );
  }
}
